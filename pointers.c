//Pointers

#include <stdio.h>
struct Distance
{
  char c;
  scanf("%s",c),
  if(c>='A' && c<='Z')
  {
      char z=c+32;
      printf("Input is uppercase\n");
      printf("Input is lowercase=%s",&z);
  }
  else if(c>='a' && c<='z')
  {
      char x=c-32;
      printf("Input is lowercase\n");
      printf("Input is uppercase=%s",&x);
  }
     return0;
  }
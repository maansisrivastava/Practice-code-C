//count and sum of odd and even numbers

#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if(a%2==0)
    printf("Sum of all odd numbers=%d\n Sum of all even numbers=%d",(a*(a)/4),{(a)*(2+a)/4});
    else 
    printf("Wrong Input");
    return0;
}